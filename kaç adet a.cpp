#include<stdio.h>
#include<string.h>
int main(){
	char cumle[100];
	printf("bir cumle giriniz: ");
	gets(cumle);
	int sayac=0;
	for (int i=0; i<strlen(cumle);i++)
	if(cumle[i]== a)
	sayac++;
	printf("girilen cumlede %d tane a harfi vardir", sayac);
	
	
	return 0;
}
